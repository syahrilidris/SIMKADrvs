<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:background="@color/common_signin_btn_light_text_default"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin">

    <Button
        android:id="@+id/btnNormal"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:width="200dp"
        android:onClick="btnTakePhotoClicked"
        android:text="CITIZEN" />

    <Button
        android:id="@+id/btnMinor"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/btnNormal"
        android:layout_alignStart="@+id/btnNormal"
        android:layout_below="@+id/btnNormal"
        android:layout_marginTop="23dp"
        android:width="200dp"
        android:onClick="btnExitClicked"
        android:text="FOREIGNER" />

    <Button
        android:id="@+id/btnCorporate"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/btnMinor"
        android:layout_alignStart="@+id/btnMinor"
        android:layout_below="@+id/btnMinor"
        android:layout_marginTop="26dp"
        android:width="200dp"
        android:onClick="btnExitClicked"
        android:text="BUSINESS ENTITY" />

</RelativeLayout>